server:
  disable: true

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://sissi_loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: system
    static_configs:
      - targets:
          - localhost
        labels:
          job: sissi-logs
          __path__: /logs/*.log